RUN the app.py, will merge both app.py and app_fixed.py
upload the sample file .csv to get locations
to use roadbased optimal path copy paste this api key in the setting tab and press save settings a green message should pop up 'settings saved' if not press it again
then go to stop manager tab and press optimize route wait till it fully loads then go to map view you should see the results
